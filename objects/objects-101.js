let myInfo = {
    name: 'Murali Uppangala',
    age: 31,
    location: 'Bangalore'
}
console.log(`${myInfo.name} is ${myInfo.age} and lives in ${myInfo.location}.`)

myInfo.age+=1

console.log(`${myInfo.name} is ${myInfo.age} and lives in ${myInfo.location}.`)